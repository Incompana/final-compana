# Model Candidates (Placeholder)

Keep this list intentionally simple for capstone scope.

## Stage 1 (default now)

- Rule-based classifier and router
- Knowledge-base-driven gap mapping
- Rule-based rubric scoring for task feedback

## Stage 2 (optional upgrade)

- TF-IDF + Logistic Regression (problem classification)
- TF-IDF + Linear SVM (comparison baseline)
- Embedding + simple linear classifier (only if Stage 2 underperforms)

## Out of scope for now

- Deep learning pipelines
- Heavy fine-tuning workflows
- Large-scale personalization models

## Selection criteria

- Better dev/test performance than rules
- Explainable errors
- Easy rollback to deterministic fallback
